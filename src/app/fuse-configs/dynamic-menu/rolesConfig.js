import * as module from "./modulesConfig";

const DIACO_USER = [module.DASHBOARD, module.QUEJAS];
const DIACO_ADMIN = [module.DASHBOARD, module.COMERCIO, module.REPORTES];

export const ROLE = {
  DIACO_ADMIN,
  DIACO_USER,
};
